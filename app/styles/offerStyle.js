import { StyleSheet } from "react-native";
const offerStyle = StyleSheet.create({
    border: {
        marginTop: 10,
        marginBottom:10,
        padding:10,
        borderWidth: 2,
        borderColor: "black",
        borderRadius: 15,
    }
});

export default offerStyle;