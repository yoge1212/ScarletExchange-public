import { StyleSheet } from "react-native";
const offerStyle = StyleSheet.create({
    border: {
        margin: 10,
        padding:10,
        borderWidth: 2,
        borderColor: "black",
        borderRadius: 15,
    },
    container: {
        width: "40%",
        height: "45%",
    }
});

export default offerStyle;